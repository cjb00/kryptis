[package]
name = "kryptis-types"
version = "0.1.0"
edition = "2021"
description = "Shared types between kryptis host and zkVM guest program"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
sha2 = "0.10"
